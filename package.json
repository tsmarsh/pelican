{
  "name": "pelican",
  "version": "1.0.0",
  "description": "A family event bingo game",
  "private": true,
  "scripts": {
    "build:ts": "tsc",
    "build:assets": "mkdir -p dist && cp src/index.html dist/index.html && cp src/style.css dist/style.css && cp src/lz-string.js dist/lz-string.js && cp src/qrcode.js dist/qrcode.js",
    "build": "yarn build:ts && yarn build:assets",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
